
// Rota para obter os dados via JSON, substituir o ano e o mês se assim for necessário
document.addEventListener("DOMContentLoaded", () => {
    console.log('Rota para os dados do Gráfico 1:', window.evolutionAnnualRoute);
});